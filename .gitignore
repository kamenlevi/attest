# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Models, data, and caches (large / local)
models/
*.gguf
*.safetensors
data/*.pdf
.cache/

# Secrets — NEVER commit. Your real key lives in .env (ignored); .env.example is the safe template.
.env
*.env
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# Downloaded test data (books, etc.) — not committed
data/
